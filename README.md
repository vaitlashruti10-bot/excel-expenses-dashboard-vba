# 1. Create the repo on GitHub first (as in step 1 above), but skip adding README

# 2. On your computer, in the project folder:
git init
git add README.md BuildExpensesDashboard.bas
git commit -m "Initial commit: Excel VBA expenses dashboard"

# 3. Connect to your GitHub repo
git remote add origin https://github.com/yourusername/excel-expenses-dashboard-vba.git
git branch -M main
git push -u origin main
